{% macro window(title, id, content) %}
  <section id="{{ id }}">
    <div class="window">
      <div class="title-bar">
        <div class="title-bar-text">{{ title }}</div>
        <div class="title-bar-controls">
          <button aria-label="Minimize" onclick="minimizeThing('{{ id }}');"></button>
          <button aria-label="Maximize"></button>
          <button aria-label="Close" onclick="hideThing('{{ id }}');"></button>
        </div>
      </div>
      <div class="window-body">
        {{ content | safe }}
      </div>
    </div>
  </section>
{% endmacro %}

{% macro windowTop(title, id) %}
  <section id="{{ id }}">
    <div class="window">
      <div class="title-bar">
        <div class="title-bar-text">{{ title }}</div>
        <div class="title-bar-controls">
          <button aria-label="Minimize" onclick="minimizeThing('{{ id }}');"></button>
          <button aria-label="Maximize"></button>
          <button aria-label="Close" onclick="hideThing('{{ id }}');"></button>
        </div>
      </div>
      <div class="window-body">
      {% endmacro %}

      {% macro windowBottom() %}
      </div>
    </div>
  </section>
{% endmacro %}

{#
{% macro collectionImages(collectionName) %}
  {% for image in collections.'{{ collectionName }}' %}
    <img src='{{ image }}' id='{{ image }}' title='This title should be replaced.'>
  {% endfor %}

  <script type="text/javascript">
    {% for image in collections.{{ collectionName }} %}
      document.getElementById('{{ image }}').setAttribute("title", createTitle("{{ image }}"));
    {% endfor %}
  </script>
{% endmacro %}
#}

{% macro collectionImages(imageName) %}
    <img src='{{ imageName }}' id='{{ imageName }}' title='This title should be replaced.'>

  <script type="text/javascript">
      document.getElementById('{{ imageName }}').setAttribute("title", createTitle("{{ imageName }}"));
  </script>
{% endmacro %}
